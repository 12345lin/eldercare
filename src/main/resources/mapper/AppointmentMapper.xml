<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wmm.eldercare.core.mapper.AppointmentMapper">

    <!-- 插入预约记录 -->
    <insert id="insert" parameterType="com.wmm.eldercare.core.pojo.Appointment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO appointment (user_id, slot_id, package_id, status, report_url, create_time, update_time, deleted)
        VALUES (#{userId}, #{slotId}, #{packageId}, #{status}, #{reportUrl}, #{createTime}, #{updateTime}, #{deleted})
    </insert>

    <!-- 根据 ID 查询预约 -->
    <select id="findById" resultType="com.wmm.eldercare.core.pojo.Appointment">
        SELECT id, user_id, slot_id, package_id, status, report_url, create_time, update_time
        FROM appointment
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 查询用户的预约记录 -->
    <select id="findByUserId" resultType="com.wmm.eldercare.core.pojo.Appointment">
        SELECT id, user_id, slot_id, package_id, status, report_url, create_time, update_time
        FROM appointment
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 更新预约状态 -->
    <update id="updateStatus">
        UPDATE appointment
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND user_id = #{userId} AND deleted = 0
    </update>

<!-- 统计某用户预约总次数（个人中心统计面板） -->
    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM appointment WHERE user_id = #{userId} AND deleted = 0
    </select>

</mapper>
