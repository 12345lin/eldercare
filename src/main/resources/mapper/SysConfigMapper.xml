<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wmm.eldercare.core.mapper.SysConfigMapper">

    <!-- 根据配置键查询配置 -->
    <select id="findByKey" resultType="com.wmm.eldercare.core.pojo.SysConfig">
        SELECT id, config_key, config_value, description, create_time, update_time
        FROM sys_config
        WHERE config_key = #{configKey} AND deleted = 0
        LIMIT 1
    </select>

</mapper>
